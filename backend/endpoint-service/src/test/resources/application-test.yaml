spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.amqp.RabbitAutoConfiguration
  rabbitmq:
    listener:
      simple:
        auto-startup: false
      direct:
        auto-startup: false

pulse:
  jwt:
    secret: test-secret-for-integration-tests-with-enough-length-for-hs256-256bits
