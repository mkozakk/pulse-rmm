server:
  port: 8080

grpc:
  server:
    port: 9090

spring:
  application:
    name: api-gateway
  threads:
    virtual:
      enabled: true
  cloud:
    gateway:
      mvc:
        routes:
          - id: identity
            uri: ${IDENTITY_SERVICE_URL:http://localhost:8083}
            predicates:
              - Path=/api/auth/**
          - id: enrolment
            uri: ${ENROLMENT_SERVICE_URL:http://localhost:8081}
            predicates:
              - Path=/api/enrolment/**
          - id: endpoint-metrics
            uri: ${METRIC_SERVICE_URL:http://localhost:8082}
            predicates:
              - Path=/api/endpoints/*/metrics
          - id: software
            uri: ${SOFTWARE_SERVICE_URL:http://localhost:8085}
            predicates:
              - Path=/api/endpoints/*/software/**
          - id: groups
            uri: ${ENROLMENT_SERVICE_URL:http://localhost:8081}
            predicates:
              - Path=/api/groups/**
          - id: tag-rules
            uri: ${ENROLMENT_SERVICE_URL:http://localhost:8081}
            predicates:
              - Path=/api/tag-rules/**
          - id: endpoints
            uri: ${ENROLMENT_SERVICE_URL:http://localhost:8081}
            predicates:
              - Path=/api/endpoints/**
          - id: scripts
            uri: ${SCRIPT_SERVICE_URL:http://localhost:8084}
            predicates:
              - Path=/api/scripts/**
          - id: api-docs-identity
            uri: ${IDENTITY_SERVICE_URL:http://localhost:8083}
            predicates:
              - Path=/api-docs/identity
            filters:
              - RewritePath=/api-docs/identity, /v3/api-docs
          - id: api-docs-enrolment
            uri: ${ENROLMENT_SERVICE_URL:http://localhost:8081}
            predicates:
              - Path=/api-docs/enrolment
            filters:
              - RewritePath=/api-docs/enrolment, /v3/api-docs
          - id: api-docs-metrics
            uri: ${METRIC_SERVICE_URL:http://localhost:8082}
            predicates:
              - Path=/api-docs/metrics
            filters:
              - RewritePath=/api-docs/metrics, /v3/api-docs
          - id: api-docs-scripts
            uri: ${SCRIPT_SERVICE_URL:http://localhost:8084}
            predicates:
              - Path=/api-docs/scripts
            filters:
              - RewritePath=/api-docs/scripts, /v3/api-docs
          - id: api-docs-software
            uri: ${SOFTWARE_SERVICE_URL:http://localhost:8085}
            predicates:
              - Path=/api-docs/software
            filters:
              - RewritePath=/api-docs/software, /v3/api-docs
      globalcors:
        cors-configurations:
          '[/**]':
            allowed-origins: "http://localhost:5173"
            allowed-methods: [GET, POST, PUT, DELETE, OPTIONS]
            allowed-headers: "*"
            allow-credentials: true
            max-age: 3600

pulse:
  jwt:
    secret: ${JWT_SECRET:change-me-in-production-set-JWT_SECRET-env-var}
  identity:
    url: ${IDENTITY_SERVICE_URL:http://identity-service:8083}
    internal-secret: ${INTERNAL_API_SECRET:change-me-in-production-set-INTERNAL_API_SECRET-env-var}

management:
  endpoints:
    web:
      exposure:
        include: health

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: ${SWAGGER_UI_ENABLED:true}
    path: /swagger-ui.html
    urls:
      - name: Identity
        url: /api-docs/identity
      - name: Enrolment
        url: /api-docs/enrolment
      - name: Metrics
        url: /api-docs/metrics
      - name: Scripts
        url: /api-docs/scripts
      - name: Software
        url: /api-docs/software
    urls-primary-name: Identity
    tags-sorter: alpha
    operations-sorter: alpha
    disable-swagger-default-url: true
