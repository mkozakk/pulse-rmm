[pytest]
markers =
    slow: slow tests (require agent container, socket operations)
    fast: fast tests (API-only)
    requires_agent: tests that require an enrolled agent
testpaths = tests/
python_files = test_*.py
